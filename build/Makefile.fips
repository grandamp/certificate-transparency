all: $(INSTALL_DIR)/bin/fips

$(INSTALL_DIR)/bin/fips: Makefile
	$(MAKE)
	$(MAKE) install

Makefile: config Configure Makefile.org
	./config --install_prefix=$(INSTALL_DIR)
	# just to be sure we don't have dregs left over
	$(MAKE) clean
